# csnl-researcher-archiver — local config (per PC)
# Fill MY_INIT with your researcher initial (must match config/researchers.yaml)
# Get PG_WORKER_PASSWORD from lab admin (JOP <jy061100@gmail.com>)

MY_INIT=
PG_HOST=csnls-mac-studio.local
PG_PORT=5432
PG_DBNAME=csnl_v3
PG_USER=harness_worker
PG_WORKER_PASSWORD=

# NAS path — if your NAS is not mounted, point to a local working dir
# (overrides lab.nas_root_default in researchers.yaml)
NAS_ROOT=

# Optional: plugin root (auto-detected by install.sh)
# PLUGIN_ROOT=

# Optional: verbose logging
# CSNL_ARCHIVE_DEBUG=1
