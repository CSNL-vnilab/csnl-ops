# csnl-researcher-archiver — local config (per PC)
# 연구원이 채워야 할 줄: MY_INIT + SUPABASE_DB_PASSWORD (2 줄). 나머지는 동일.

# === REQUIRED ===

# 본인 이니셜 (대소문자 정확히 — config/researchers.yaml 의 active 항목과 일치)
MY_INIT=

# 운영자가 별도 채널로 전달한 DB 비밀번호 (service_role JWT 가 아닌 DB user 비밀번호)
SUPABASE_DB_PASSWORD=

# === FIXED (CSNL 프로젝트 공용 — 수정 불필요) ===
SUPABASE_DB_HOST=aws-1-ap-southeast-1.pooler.supabase.com
SUPABASE_DB_PORT=5432
SUPABASE_DB_USER=postgres.qjhzjqkrbvsnwlbpilio

# === OPTIONAL ===

# NAS 마운트 경로 — NAS 미마운트 또는 로컬-only 모드면 비워둠
NAS_ROOT=/Volumes/CSNL_new

# 디버그 로깅
# CSNL_ARCHIVE_DEBUG=1
