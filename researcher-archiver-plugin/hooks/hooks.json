{
  "$schema": "https://docs.claude.com/claude-code/hooks/v1",
  "comment": "csnl-researcher-archiver hooks. Pre-fire tone lint + SessionEnd auto-handoff. Both are non-blocking (lint logs violations to stderr, doesn't kill the op).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": { "tool": "Write" },
        "comment": "Run tone lint on Write content. Exit=1 logs violations but doesn't block.",
        "command": "python3 \"${PLUGIN_ROOT}/hooks/pre-fire-lint.py\""
      }
    ],
    "SessionEnd": [
      {
        "comment": "Auto-write handoff-<datetime>-autosave.md if user didn't run /archive:handoff",
        "command": "\"${PLUGIN_ROOT}/hooks/auto-handoff.sh\" \"${MY_INIT:-}\""
      }
    ]
  }
}
