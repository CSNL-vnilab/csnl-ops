# DM Draft — JYK (김정예) — NAS-grounded uncertainty Q

**Status**: DRAFT — not yet sent. Awaiting user pre-check per `feedback_first_run_external` memory rule.

## Routing
- Recipient: JYK (김정예)
- Channel: 1:1 DM
- Send mode: **top-level DM**
- Send kind tag: `nas_grounded_uncertainty_Q`
- Estimated len: ~350 chars

## Context (why this draft, why NAS-grounded)

- 2026-05-08 inbound: "관련 지식 잘 모르고 아직 해본적 없음" (RNN WM modeling)
- NAS source verified: `/Volumes/CSNL_new-1/Memory/JYK/RNN/Results/trained_rnn/` (4 folders: anchora20, anchora90, homogeneous/heterogeneous variants)
- NAS-grounded uncertainty surfaced: **anchor model 학습 시 α(alpha) 파라미터 범위**

## Draft body

```
김정예 연구원께,

`/Volumes/CSNL_new-1/Memory/JYK/RNN/Results/trained_rnn/` 확인 결과, 학습된 RNN 모델 폴더가 anchora20 (α=2.0) 과 anchora90 (α=9.0) 두 극단값만 존재합니다.

모델 폴더명 구조상 α 값이 명시되어 있는데 (예: heterogeneous_anchor_anchora20_anchorg10_anchorp2_emonly), 현재 저장된 모델은 anchora20 과 anchora90 두 가지뿐입니다. 중간 α 값 (예: 4, 5, 6 또는 다른 값) 범위에서 (a) 이미 학습했으나 다른 폴더에 저장, (b) 아직 학습 안 함, (c) 학습 계획 중 인지 궁금합니다.

특히 α 의 효과를 탐색할 때 극단값 2 개로만 충분한지, 아니면 중간값 sampling 이 필요한지에 대한 현재 계획을 알고 싶습니다.

---

(NAS 데이터 사용 정책 — 본 질문은 위 trained_rnn/ 디렉터리 구조의 read 결과를 기반으로 합니다. 본 형태의 NAS 탐사가 불편하시면 (P1)~(P5) 중 하나로 회신해 주십시오.)

(P1) 본 모델 폴더 정보만 메모리 저장 X. 즉, α 값 및 모델 파라미터가 영구 저장되지 않고 한 번 read 후 즉시 폐기.
(P2) `/Volumes/CSNL_new-1/Memory/JYK/RNN/Results/trained_rnn/` 디렉터리 더 이상 탐사 X. 블락 영향: RNN anchor model hyperparameter sweep 자동 follow-up 불가, α 값 선택 기준 평가 불가.
(P3) `JYK/RNN/` 프로젝트 전체 탐사 X. 블락 영향: RNN WM modeling 및 시계열 bias-variance 모델 paper rec scoring 불가, carry-over alternates 의 RNN 기반 re-rank 불가.
(P4) `JYK/` 전체 NAS 탐사 X. 블락 영향: paperblitz / 개인화 자동화 모두 제거. (Slack DM 기본 ack 와 reminder 는 유지.)
(P5) 본 draft 자체 보류.

— Claude
```

## Lint pre-check (slack_outbound _enforce_or_raise)

- emoji / shortcode: 없음 ✓
- flattery: 없음 ✓
- intensifier: 없음 ✓
- emotive punct: 없음 ✓
- greeting boilerplate: 없음 ✓
- Expected lint result: clean

## Risks for user pre-check

1. **두 가지 α 값만 존재함이 실제로 확인됨**: find 결과에서 anchora20_* 와 anchora90_* 만 나타남. 다른 α 값의 모델 폴더는 trained_rnn/ 디렉터리에 없음 (verified).
2. **중간 α 값 학습 여부가 불명확**: 다른 곳 (예: scratch 디렉터리 또는 다른 경로) 에 저장되었을 가능성도 있음. 따라서 질문 형식이 (a)/(b)/(c) 로 적절.
3. JYK의 "관련 지식 잘 모르고 아직 해본적 없음" 답신으로 보아, 본 질문은 현재 학습 상태 파악 및 계획 수립에 유용한 정보.
