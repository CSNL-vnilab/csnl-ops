# DM Draft — MSY (여민수) — NAS-grounded uncertainty Q

**Status**: DRAFT — not yet sent. Awaiting user pre-check per `feedback_first_run_external` memory rule.

## Routing
- Recipient: MSY (여민수)
- Channel: 1:1 DM
- Send mode: **top-level DM**
- Send kind tag: `nas_grounded_uncertainty_Q`
- Estimated len: ~420 chars

## Context (why this draft, why NAS-grounded)

- 2026-05-08 inbound: "이 논문에 관한 질문을 해도 되나?" (CatVsMag deep-gen face)
- NAS source verified: `/Volumes/CSNL_new-1/Memory/MSY/Code/cat_mag_main/analysis.ipynb` (2377 lines, TwoGaussianFit 함수 사용 detected)
- NAS-grounded uncertainty surfaced: **Bayesian observer 모델의 crop 파라미터 미정**

## Draft body

```
여민수 연구원께,

`/Volumes/CSNL_new-1/Memory/MSY/Code/cat_mag_main/analysis.ipynb` 확인 결과, TwoGaussianFit 함수의 crop 파라미터 설정이 명확하지 않습니다.

분석 코드에서 `TwoGaussianFit` 호출 시 `upper_crop` 과 `lower_crop` 값이 두 가지 조합으로 실험되어 있습니다:
- (0.98, 0.02): notebook line 1492 부근
- (0.96, 0.04): notebook line 1689 부근

또한 plotting 부분 (line 1513-1514) 에서 두 gaussian 을 따로 plot 하는 코드가 주석 처리되어 있어서, 최종 모델이 (a) 두 gaussian 의 weighted sum 인지, (b) 각 gaussian 을 독립적으로 다루는지, (c) 두 조합 중 하나를 선택했는지 불명확합니다.

현재 분석의 최종 crop 파라미터 (0.98 vs 0.96) 선택 기준과, 최종 모델 형식 (weighted sum vs independent) 이 궁금합니다.

---

(NAS 데이터 사용 정책 — 본 질문은 위 notebook 파일 1 개의 read 결과를 기반으로 합니다. 본 형태의 NAS 탐사가 불편하시면 (P1)~(P5) 중 하나로 회신해 주십시오.)

(P1) 본 분석 코드만 메모리 저장 X. 즉, TwoGaussianFit 의 파라미터 값들이 영구 저장되지 않고 한 번 read 후 즉시 폐기.
(P2) `/Volumes/CSNL_new-1/Memory/MSY/Code/cat_mag_main/analysis.ipynb` 파일 더 이상 탐사 X. 블락 영향: cat_mag_main 자동 follow-up 불가, Bayesian observer 확장 가능성 평가 및 모델 파라미터 scoring 불가.
(P3) `MSY/` 전체 NAS 탐사 X. 블락 영향: face perception / CatVsMag paper rec scoring 불가, carry-over alternates 의 MSY 기반 re-rank 불가.
(P4) (P3 과 동일).
(P5) 본 draft 자체 보류.

— Claude
```

## Lint pre-check (slack_outbound _enforce_or_raise)

- emoji / shortcode: 없음 ✓
- flattery: 없음 ✓
- intensifier: 없음 ✓
- emotive punct: 없음 ✓
- greeting boilerplate: 없음 ✓
- Expected lint result: clean

## Risks for user pre-check

1. **crop 파라미터 이중 설정이 notebook 에서 정확히 확인됨**: (0.98, 0.02) 와 (0.96, 0.04) 모두 실제로 line 근처에서 호출됨 (verified by grep).
2. **주석 처리된 plotting 코드**: line 1512 에 weighted-sum plotting 코드가 있고, line 1513 은 그것을 주석 처리하고 개별 gaussian plot 로 변경. 이는 실제 설계 변경을 시사.
3. MSY의 "이 논문에 관한 질문을 해도 되나?" 답신으로 보아 심층 분석에 대해 관심 있음.
